services:
  mysql:
    image: mysql:8.4
    container_name: stock-control-db
    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: 892240
      MYSQL_DATABASE: stock_control_db

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: